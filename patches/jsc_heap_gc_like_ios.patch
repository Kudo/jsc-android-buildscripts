--- target-org/webkit/Source/WTF/wtf/PlatformUse.h	2020-01-28 09:30:41.000000000 +0800
+++ target/webkit/Source/WTF/wtf/PlatformUse.h	2020-07-20 11:12:02.000000000 +0800
@@ -304,8 +304,10 @@
 #define USE_PLATFORM_SYSTEM_FALLBACK_LIST 1
 #endif

-#if PLATFORM(IOS_FAMILY) || (!(defined(USE_SYSTEM_MALLOC) && USE_SYSTEM_MALLOC) && (OS(LINUX) && (PLATFORM(GTK) || PLATFORM(WPE))))
+#if PLATFORM(IOS_FAMILY) || (!(defined(USE_SYSTEM_MALLOC) && USE_SYSTEM_MALLOC) && (OS(LINUX) && (PLATFORM(GTK) || PLATFORM(WPE)))) || __ANDROID__
 #define USE_BMALLOC_MEMORY_FOOTPRINT_API 1
+#else
+#error "NO USE_BMALLOC_MEMORY_FOOTPRINT_API"
 #endif

 #if OS(DARWIN) && !USE(PLATFORM_REGISTERS_WITH_PROFILE) && CPU(ARM64)
